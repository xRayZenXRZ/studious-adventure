info:
  name: Health
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/health"
  auth: inherit

runtime:
  assertions:
    - expression: res.body
      operator: isJson
    - expression: res.body.success
      operator: isTruthy
    - expression: res.body.message
      operator: eq
      value: World Cup Ticketing API
    - expression: res.body.uptime
      operator: isDefined
    - expression: res.body.environment
      operator: isDefined
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body.uptime
      operator: isDefined
    - expression: res.headers['content-type']
      operator: eq
      value: application/json
    - expression: req.method
      operator: eq
      value: GET

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
